# 🤝 贡献指南

感谢你有兴趣为 DesktopWidget 贡献主题！本文件说明了提交主题到本仓库的**目录结构**、**文件要求**和**提交流程**。

> 提交前请先阅读[主题制作开发指南](https://github.com/Cherish95279/DesktopWidget/blob/main/docs/THEME_DEV_GUIDE.md)，了解画布规格、图层顺序和指针枢轴要求。

---

## 目录

- [1. 前置要求](#1-前置要求)
- [2. 目录结构](#2-目录结构)
- [3. 文件要求](#3-文件要求)
- [4. 命名规范](#4-命名规范)
- [5. 设计建议](#5-设计建议)
- [6. 提交流程](#6-提交流程)
- [7. 审核流程](#7-审核流程)
- [8. 更新主题索引](#8-更新主题索引)
- [9. 测试清单](#9-测试清单)

---

## 1. 前置要求

- DesktopWidget v1.5.0 或更高版本（用于本地导入测试）
- 支持 PNG RGBA 透明通道的图像编辑器（如 Photoshop、GIMP、Krita、Aseprite）
- 已阅读[主题制作开发指南](https://github.com/Cherish95279/DesktopWidget/blob/main/docs/THEME_DEV_GUIDE.md)

---

## 2. 目录结构

每个主题是 `themes/` 下的一个文件夹，包含 2~5 张 PNG 图片：

```
themes/
└── 你的主题名/
    ├── bg.png            ← 必需，400×297
    ├── face.png          ← 必需，400×297
    ├── Hour_Hand.png     ← 可选，400×297
    ├── Minute_Hand.png   ← 可选，400×297
    └── Second_Hand.png   ← 可选，400×297
```

### 规则

- **文件夹名**即主题显示名（出现在下拉框中），支持任意语言，包括中文
- `bg.png` 和 `face.png` **必须同时存在**，否则校验不通过
- 指针图片可选，缺失时回退默认主题的指针
- 不要提交非 PNG 文件（不要 `.svg`、`.psd`、`.xcf` 等）

---

## 3. 文件要求

| 文件 | 必须 | 尺寸 | 格式 | 说明 |
|------|:----:|------|------|------|
| `bg.png` | ✅ | 400×297 | PNG (RGBA) | 背景层，受用户颜色着色 |
| `face.png` | ✅ | 400×297 | PNG (RGBA) | 表盘层，原样绘制（不着色） |
| `Hour_Hand.png` | ❌ | 400×297 | PNG (RGBA) | 时针，0° 指向上方，枢轴 (199,143) |
| `Minute_Hand.png` | ❌ | 400×297 | PNG (RGBA) | 分针，同上 |
| `Second_Hand.png` | ❌ | 400×297 | PNG (RGBA) | 秒针，同上 |

### 关键规格

- **所有图片必须精确为 400×297 像素**
- **格式必须为 PNG，带 RGBA 透明通道**
- **指针图片**：未旋转状态（0°）下指针必须指向**正上方**（12 点钟方向）
- **指针枢轴**：旋转轴心在图片内部的 **(199, 143)** 像素位置
- 透明区域必须 alpha=0（不能填白色）

---

## 4. 命名规范

| 项目 | 规则 | 示例 |
|------|------|------|
| 主题文件夹名 | 任意语言，描述性 | `Neon Clock`、`简约时光`、`日落黄昏` |
| 图片文件名 | 固定英文名，区分大小写 | `bg.png`、`face.png`、`Hour_Hand.png` |
| ZIP 文件名 | `<主题名>_v<版本>.zip` | `简约时光_v1.0.0.zip` |

> 文件夹名支持空格和 Unicode。ZIP 文件名建议将空格替换为下划线。

---

## 5. 设计建议

### 5.1 背景层（bg.png）

- 此层会被用户选定的颜色通过 `SourceAtop` 混合模式**着色**
- 建议使用半透明或中性色，以获得最佳着色效果
- 透明区域着色后仍保持透明
- 避免纯白（#FFFFFF）或纯黑（#000000），会降低着色效果

### 5.2 表盘层（face.png）

- **原样绘制**，不受着色和不透明度影响
- 适合放：表盘刻度、数字、装饰边框、钟表中心装饰
- 主题的视觉风格主要体现在这一层

### 5.3 指针（时针/分针/秒针）

- 每张指针图片 400×297，指针从枢轴 (199,143) 向**上方**延伸
- 画布其余区域必须完全透明
- 0° 旋转时指针指向 12 点钟方向
- 图片中的 (199,143) 像素点对齐画布中心 (201,144)

### 5.4 文字槽位区域

程序会在图片之上绘制 8 个文字信息，**设计时应避开这些区域**或保证足够对比度：

| 槽位 | X | Y | 宽 W | 高 H | 位置 |
|------|---|---|------|------|------|
| 1 | 20 | 30 | 105 | 43 | 左上 |
| 2 | 20 | 86 | 85 | 43 | 左侧 |
| 3 | 20 | 166 | 70 | 50 | 左侧 |
| 4 | 20 | 235 | 88 | 50 | 左下 |
| 5 | 280 | 30 | 94 | 43 | 右上 |
| 6 | 314 | 86 | 71 | 43 | 右侧 |
| 7 | 324 | 166 | 60 | 50 | 右侧 |
| 8 | 273 | 238 | 97 | 43 | 右下 |

---

## 6. 提交流程

### 6.1 Fork & Clone

```bash
git clone https://github.com/<你的用户名>/DesktopWidget-Themes.git
cd DesktopWidget-Themes
```

### 6.2 添加主题

1. 在 `themes/` 下创建以你的主题名命名的文件夹
2. 添加 `bg.png` 和 `face.png`（必需）以及指针图片（可选）
3. 打包为 ZIP：压缩文件夹，使 ZIP 内包含一级子目录
4. 在 DesktopWidget 中导入 ZIP 测试显示效果

### 6.3 提交 PR

```bash
git checkout -b add-<主题名>
git add themes/<主题名>/
git commit -m "Add theme: <主题名称>"
git push origin add-<主题名>
```

然后在本仓库发起 Pull Request。

### 6.4 PR 描述模板

```markdown
## 主题信息
- **名称**：xxx
- **作者**：xxx
- **版本**：1.0.0

## 描述
简要说明主题的视觉风格和设计理念。

## 包含文件
- [x] bg.png
- [x] face.png
- [ ] Hour_Hand.png
- [ ] Minute_Hand.png
- [ ] Second_Hand.png

## 预览
（主题效果截图或描述）

## 测试结果
- [ ] 已在 DesktopWidget 中导入测试通过
- [ ] 背景着色效果正常
- [ ] 指针旋转正确（如包含）
- [ ] 文字槽位区域可读
```

---

## 7. 审核流程

```
PR 提交
  │
  ▼
① 检查文件结构：文件夹名、必需文件、图片格式
  │
  ▼
② 验证图片规格：400×297、RGBA、指针朝向/枢轴
  │
  ▼
③ 视觉审核：美观度、文字槽位对比度、着色效果
  │
  ▼
④ 导入测试：在 DesktopWidget 中导入并验证显示
  │
  ▼
✅ 审核通过 → 合并 PR
  │
  ▼
⑤ 打包 ZIP，发布到 GitHub Release
  │
  ▼
⑥ 更新 themes/index.json 和 README 主题列表
```

### 审核说明

- 主题是**纯图片素材**，不包含代码，不存在安全风险
- 审核重点是**视觉质量**和**技术正确性**（尺寸、格式、指针朝向）
- 一般在 **2 个工作日内**完成初审

---

## 8. 更新主题索引

新增或更新主题后，请同步更新 `themes/index.json`：

```json
{
  "name": "主题名称",
  "author": "作者",
  "version": "1.0.0",
  "preview_url": "themes/主题名称/face.png",
  "download_url": "https://github.com/Cherish95279/DesktopWidget-Themes/releases/download/主题名_v1.0.0/主题名_v1.0.0.zip",
  "source_path": "themes/主题名称"
}
```

> `download_url` 指向 GitHub Release 资产地址，发布 Release 后填写。

---

## 9. 测试清单

提交前请逐项确认：

- [ ] `bg.png` 和 `face.png` 同时存在
- [ ] 所有图片精确为 400×297 像素
- [ ] 所有图片为 PNG 格式，带 RGBA 透明通道
- [ ] 指针图片在 0° 时指向正上方（12 点）
- [ ] 指针枢轴在图片内 (199, 143) 像素位置
- [ ] 文件夹中不含非 PNG 文件
- [ ] 文字槽位区域有足够对比度
- [ ] 已打包 ZIP 并在 DesktopWidget 中导入测试通过
- [ ] 已更新 `themes/index.json`

---

> 如有疑问，请在 PR 中留言或参考[主题制作开发指南](https://github.com/Cherish95279/DesktopWidget/blob/main/docs/THEME_DEV_GUIDE.md)。
